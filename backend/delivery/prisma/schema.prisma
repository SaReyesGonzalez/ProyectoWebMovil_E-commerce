generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Repartidor {
  id     Int     @id @default(autoincrement())
  userId      Int     @unique     //id del usuario al que referencia este Repartidores
  calificacion    Float?
}